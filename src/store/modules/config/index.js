import persistentVolumeClaim from '@/store/modules/config/persistentVolumeClaim'
import configMap from '@/store/modules/config/configMap'
import secret from '@/store/modules/config/secret'

export default {
  persistentVolumeClaim,
  configMap,
  secret,
}
